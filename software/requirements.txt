# Python Dependencies for Prova_Delfi
# Raspberry Pi compatible versions

# Core numerical libraries
numpy==1.26.4  # Downgraded for compatibility with Raspberry Pi and tflite-runtime
scipy
pillow
opencv-python==4.8.1.78  # Pinned for numpy 1.26.4 compatibility

# Communication
pyserial

# Web interface
flask

tflite-runtime-nightly
# TFLite Runtime (requires manual installation on Raspberry Pi)
# tflite-runtime-nightly
# NOTE: tflite-runtime 2.18.0 must be installed manually via community build
# https://github.com/tensorflow/tensorflow/releases/download/v2.18.0/tflite_runtime-2.18.0-cp311-cp311-linux_aarch64.whl
